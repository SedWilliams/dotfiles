return {
  {
    "vim-airline/vim-airline",
    dependencies = { "vim-airline/vim-airline-themes" },
    config = function()
      -- Airline config is already in init.vim, but can be moved here if desired.
    end,
  },
  { "andweeb/presence.nvim" },
  { "preservim/nerdtree" },
}
