#!/bin/bash

# This script is the master install script which installs all of the dotfiles from the repo.
# UIMPLEMENTED, add logic to check for existing installations and also error handling to fail safely
# Optionally add optional install selections
